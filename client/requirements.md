## Packages
framer-motion | Essential for the "wiggle" and hand-drawn animations
clsx | For conditional class names
tailwind-merge | For merging tailwind classes

## Notes
The API manifest only provided GET endpoints (list and get).
This frontend is built as a "Personal Website" viewer.
No admin/write capabilities are implemented as no mutation endpoints were provided in the routes manifest.
Images are handled with SVGs or placeholders as requested.
